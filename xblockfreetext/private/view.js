function XblockFreetextView(runtime, element) {
    'use strict';

    var $ = window.jQuery;
    var $element = $(element);

    // TODO: Put your logic here

    // To find elements inside your XBlock, try:
    // var myElement = $element.find('.myElement');
}
